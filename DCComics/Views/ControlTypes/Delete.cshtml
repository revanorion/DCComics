@model DCComics.Model.ControlType

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>ControlType</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ControlTypeName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ControlTypeName)
        </dd>

    </dl>

    @using (Html.BeginForm("Delete", "ControlTypes", new { id = Model.ControlTypeId }, FormMethod.Post))
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color container-fluid">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Delete" class="btn btn-danger" />
            </div>
        </div>
    }
</div>
